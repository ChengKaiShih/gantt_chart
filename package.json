{
  "name": "engineering-gantt-planner",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check dist/app.js && node --check dist/schedule-core.js"
  }
}
